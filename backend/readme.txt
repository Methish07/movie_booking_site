json web token

jwt.sign(payload,secret_key) will return a encrypted token
payload can be any data type like string,object etc
secret_key will be the secret key we are defining

this is done at backend to create a token